---
import BaseLayout from '~/layouts/BaseLayout.astro';
import InfoSection from '~/components/InfoSection.astro';
import { withFallbackSeo } from '~/lib/seo';

export const prerender = true;

const seo = withFallbackSeo([], {
  title: 'La storia | Multimage',
  description: 'La timeline editoriale sarà disponibile qui a breve.',
});
---

<BaseLayout seo={seo}>
  <InfoSection
    title="La storia"
    paragraphs={["La timeline editoriale sarà disponibile qui a breve."]}
    chip="Aggiornamento in arrivo"
  />
</BaseLayout>
