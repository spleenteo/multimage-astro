---
import BaseLayout from '~/layouts/BaseLayout.astro';
import InfoSection from '~/components/InfoSection.astro';
import { withFallbackSeo } from '~/lib/seo';

export const prerender = true;

const seo = withFallbackSeo([], {
  title: 'Contatti | Multimage',
  description: 'Inseriremo riferimenti aggiornati per contattarci.',
});
---

<BaseLayout seo={seo}>
  <InfoSection
    title="Contatti"
    paragraphs={['Inseriremo riferimenti aggiornati per contattarci.']}
    chip="Aggiornamento in arrivo"
  />
</BaseLayout>
