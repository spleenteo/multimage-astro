
## Security
- [ ] [Security] [Impact: H] [Effort: L] [Owner: @codex] [Status: todo] — **S5** retire `/api/post-deploy` (or enforce a server-side secret) so malicious calls cannot leak `SECRET_API_TOKEN` through preview webhooks (Due: 2025-11-15; Acceptance: route removed or 401s unless an internal secret is present, docs/guidelines/project-structure.md#api--preview-routes and docs/guidelines/seo.md#preview-plugins--automation updated).
- [ ] [Security] [Impact: H] [Effort: M] [Owner: @codex] [Status: wip] — **S2** sanitize every `set:html` surface fed by `toRichTextHtml` (home hero, BannerSection, Cta blocks, book descriptions, supplier bios) (Due: 2025-12-05; Acceptance: introduce a shared HTML sanitizer, add regression tests for script injection, cross-reference docs/guidelines/assets.md#html-fragments--sanitization). **Partial (2026-05-30 → 2026-05-31)**: WYSIWYG fields rely on restricted toolbars to strip paste-from-Word at input time. The `sanitized_html` validator has been removed from **all 16** WYSIWYG fields (it re-surfaced on 7 and blocked saves / caused dirty-form loops); policy is now **no `sanitized_html` on WYSIWYG fields** — see decision-log §4. Still open: render-time sanitization of `set:html`/`toRichTextHtml` for legacy values and all other surfaces.
- [ ] [Security] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **S6** add an SSR regression test that rejects double-encoded paths (coverage for the Astro 5.16.3 path-decoding patch); exercise the SSR + ISR request path (the `SERVER=static`/`SERVER=preview` dual-runtime was removed in the unify-projects-isr cutover) (Due: 2025-12-09; Acceptance: test fails on 5.15, passes on 5.16.3+, documented in docs/guidelines/testing.md).
- [ ] [Security] [Impact: M] [Effort: M] [Owner: @codex] [Status: todo] — **S3** add CSP/SRI + bundle integrity for Iconify, GA, Iubenda, and `/generated/*.js` (Due: 2025-11-28; Acceptance: BaseLayout emits CSP headers + `<script>` hashes, `/cerca` verifies generated bundles exist before booting, see docs/guidelines/assets.md and docs/guidelines/search.md).

## Code Health
- [ ] [Refactoring] [Impact: M] [Effort: S] [Owner: @claude] [Status: todo] — **D4-R5** catch `ApiError` da `@datocms/cda-client` ai boundary giusti per non far esplodere route SSR (`SERVER=preview`) su errori CDA temporanei (Acceptance: layout SSR + route SSR principali catturano `ApiError`, fallback graceful; SSG resta unchanged perché build-time errors sono già visibili).
- [ ] [Refactoring] [Impact: L] [Effort: S] [Owner: @claude] [Status: todo] — **D4-R6** aggiungere `security: { checkOrigin: false }` a `astro.config.mjs` per disambiguare il funzionamento di `/api/preview-links` quando il deploy gira in SSR mode (Acceptance: opzione presente, commit riferisce alla skill `datocms-frontend-integrations`).
- [ ] [Documentation] [Impact: M] [Effort: S] [Owner: @claude] [Status: todo] — **D4-R7** scriptare `datocms schema:inspect` per auto-generare `docs/list-models.md`, hook in `npm run docs:inventory` (Acceptance: `list-models.md` rigenerato da CLI v4, drift CMS↔docs eliminato).
- [ ] [Refactoring] [Impact: L] [Effort: S] [Owner: @claude] [Status: todo] — **D4-R9** valutare spostamento `DRAFT_MODE_COOKIE_NAME` a `context: 'client'/access: 'public'` per allinearsi alla skill canonica (decidere se mantenere server-only o standardizzare; impatto runtime zero finché nessun client code lo legge).
- [ ] [Refactoring] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **CH2** add runtime assertions (and unit tests) that `/cerca` receives valid `data-*` config before wiring listeners to avoid silent failures when the token/min-length is missing (Due: 2025-11-18; Acceptance: search bundle throws a descriptive error, tests live next to `search-page.client.ts`).
- [ ] [Refactoring] [Impact: M] [Effort: M] [Owner: @codex] [Status: todo] — **CH1** extract the inline CSV exporter in `src/pages/staff/archivio-catalogo/index.astro` into a tested helper shared with future staff tools (Due: 2025-11-25; Acceptance: helper lives under `src/lib` or `scripts/`, unit tests cover escaping/UTF-8 BOM, docs/list-helpers.md entry updated).

## Performance
- [ ] [Performance] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **P1** validate the Astro 5.16 `experimental.svgo` flag against Iconify glyphs and inline SVG artwork; enable it only if snapshot tests confirm no visual regressions and bundle size drops (Due: 2025-12-10; Acceptance: before/after size notes captured in docs/dependencies.md or docs/decision-log and the flag is toggled accordingly).

## Accessibility
- [ ] [Accessibility] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **A11Y2** add a skip link + landmark outline to BaseLayout so users can jump past the persistent header stacks (Due: 2025-11-25; Acceptance: `<a href="#main">` (or similar) is focus-visible, BaseLayout exposes `<main id="main">`, documented in docs/guidelines/project-structure.md).
- [ ] [Accessibility] [Impact: M] [Effort: M] [Owner: @codex] [Status: todo] — **A11Y1** run a WCAG 2.1 AA keyboard + screen reader audit on BookCarouselSection, `/cerca`, and staff tables, then document findings once `docs/guidelines/accessibility.md` is editable (Due: 2025-12-02; Acceptance: issues logged with repro steps, aria attributes adjusted as needed, docs/TODO cross-linked when docs open up).

## SEO
- [ ] [SEO] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **SEO1** enforce `PUBLIC_SITE_URL` (or `astro.config.site`) during builds so `sitemap.xml` and canonical URLs never fall back to `http://localhost:4321` (Due: 2025-11-18; Acceptance: build fails with a clear error when the env is missing, docs/guidelines/assets.md and docs/guidelines/project-structure.md reference the requirement).
- [ ] [SEO] [Impact: M] [Effort: M] [Owner: @codex] [Status: todo] — **SEO2** add JSON-LD for books, authors, and magazine posts (Due: 2025-12-05; Acceptance: `Book`, `Article`, and `Organization` schemas render via `<script type="application/ld+json">`, draw data from existing queries, and the implementation is documented in docs/guidelines/seo.md#structured-data--rich-results).

## CMS/Data
- [ ] [Features] [Impact: M] [Effort: M] [Owner: @codex] [Status: wip] — **CD1** restore preview/draft mode by teaching `executeQuery` to honor `includeDrafts` + alternate CDA tokens, then wiring `/api/preview` + DraftMode toggles (Due: 2025-11-28; Acceptance: preview cookies flip the token, docs/list-helpers.md and docs/guidelines/project-structure.md updated, regression tests cover both modes — regression tests still pending).
- [ ] [Refactoring] [Impact: M] [Effort: M] [Owner: @codex] [Status: todo] — **CD3** add cache-tag plumbing to the GraphQL loaders so we can revalidate only the pages touched by a publish event (Due: 2025-12-12; Acceptance: shared helpers accept `cacheTags`/`tagAll` options, routes annotate their queries, and docs/guidelines/project-structure.md#known-gaps--violations records the new policy). **Note (2026-06-20)**: the "revalidate only touched pages" goal is now met via a URL map (`revalidationUrls.ts`, see FOT decision-log); CD3 (true DatoCMS cache tags) remains the cleaner long-term approach.
- [ ] [Refactoring] [Impact: M] [Effort: H] [Owner: @codex] [Status: wip] — **CD2** replace ad-hoc `first: 500` GraphQL calls with paginated loaders + cache tags for books, authors, sitemap, and staff exports (Due: 2025-12-05; Acceptance: helpers accept `first/skip`, sitemap iterates pages, staff exports stream data in batches, docs/list-models.md reflects the new approach). **Partial (2026-06-20)**: `/libri` catalogue + its sitemap entries are paginated via `first/skip` + `_allBooksMeta` (`BOOKS_PER_PAGE`/`buildBooksCataloguePaths` in `src/lib/books.ts`). Still open: `/autori`, staff exports, and remaining `first: 500` reads.

## Testing / CI
- [ ] [Testing] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **TC1** add a GitHub Action (or Vercel check) that runs `npm run lint` + `npm run build` on every PR (Due: 2025-11-14; Acceptance: workflow file lives under `.github/workflows`, failures block merges, README/docs/guidelines/testing.md updated once editable).
- [ ] [Testing] [Impact: M] [Effort: M] [Owner: @codex] [Status: todo] — **TC2** create targeted tests for `search-page.client.ts` (debounce, filters, exact match) and the extracted CSV helper (Due: 2025-11-28; Acceptance: tests run via `npm test`, cover success + error paths, referenced in docs/guidelines/search.md and docs/list-helpers.md).

## i18n
- No open work — Multimage confirmed the site will remain permanently Italian-only (see docs/guidelines/i18n.md).

## Project Structure
- [ ] [Refactoring] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **PS1** add a prebuild verification that fails the build when `public/generated/search-page.client.js` or `swiper-element.js` are missing or stale (Due: 2025-11-18; Acceptance: CI step or `astro build` guard runs before deployment, docs/guidelines/assets.md and docs/guidelines/project-structure.md updated).
- [ ] [Refactoring] [Impact: M] [Effort: L] [Owner: @codex] [Status: todo] — **PS2** fix `LinkToRecord.astro` so `BlogPostRecord` links point to `/magazine/${slug}` and add coverage to stop regressions (Due: 2025-11-21; Acceptance: Structured Text links resolve correctly, docs/list-components.md#structured-text-blocks annotated, regression test in place).

## Documentation Hygiene
- [ ] [Documentation] [Impact: L] [Effort: M] [Owner: @codex] [Status: todo] — **DH2** wire `npm run docs:inventory` into CI so `docs/list-*` files stay in sync with the filesystem (Due: 2025-12-05; Acceptance: script runs in CI, failures block merges, referenced in docs/guidelines/testing.md).

## Completed
- 2026-06-20 - [Performance] **FOT reduction** (decision-log `2026-06-20-fot-reduction`): cut Vercel Fast Origin Transfer (Hobby 10 GB cap; `multimage-astro` was 96.4% of ~7.5 GB). (1) **Surgical revalidation** — `/api/revalidate` parses the DatoCMS webhook (`webhookPayload.ts`) and maps `api_key + slug` → ~12–20 affected URLs (`revalidationUrls.ts`) instead of re-fetching all ~670; book changes also revalidate their authors'/collection's detail pages; unknown models no-op; `?mode=full` keeps the full sweep; removed the ineffective in-memory debounce. (2) **ISR TTL 7 → 60 days** (safety net only). (3) **Lean card images** — new `RESPONSIVE_IMAGE_CARD_FIELDS` (no base64/srcSet) + `srcSetCandidates={[1,2]}` on card `<Image>`s. (4) **Paginated `/libri`** — 40/page, page 1 `/libri`, pages 2..N `/libri/pagina/<n>`; shared `BooksCatalogSection` + `Pagination` components; sitemap + revalidation updated. Local `/libri` dropped ~1.04 MB → 321 KB (dev, unminified), 0 base64, 2-candidate srcset. Build + `astro check` clean; parser unit-tested.
- 2026-06-20 - [Security] **S4 — Remove `/llms-full.txt`**: deleted the 866 KB public plaintext export (`src/pages/llms-full.txt.ts` + `src/pages/llms-full/`) and its `STATIC_URLS` entry. Anonymous visitors can no longer download the full catalogue + biographies. (Done as part of the FOT reduction work above.)
- 2026-05-31 - [Security] **S1 — Editor session gating**: le rotte interne (`/staff`, `/staff/archivio-catalogo`, `/libri/schede/*`) sono ora protette centralmente in `src/middleware.ts` e rispondono **404** senza la sessione editor (cookie JWT firmato di draft mode), invece di essere pubbliche o di dare 500. Il cookie draft = sessione editor: sblocca bozze + accesso strumenti staff + affordance (badge "Modalità editor" in BaseLayout, link "Scheda libro (staff)" su `/libri/[slug]`). Aggiunta scadenza 30 giorni al JWT/cookie (prima eterno). Attivazione via magic link esistente `/api/draft-mode/enable`. Vedi decision-log §`2026-05-31-editor-session-gating`.
- 2026-03-05 - [Documentation] **DH1**: Removed agent_edit system; consolidated root status docs into docs/current-state.md; removed DATOCMS.md (replaced by skills); simplified sync-datocms.mjs; rewrote CLAUDE.md for Claude Code workflow.
- [x] [Features] [Impact: M] [Effort: M] [Owner: @codex] [Status: done] — **I18N1** (Completed: 2025-11-07) — Scope officially locked to a single Italian locale.
- 2025-11-07 - [Refactoring] **PS3**: Implemented Draft Mode UI, `/api/preview`, and documentation so editors can use preview without removing static output.
- 2025-11-16 - [Refactoring] **PS4**: Finalised the SERVER=static/preview contract so production ships pure SSG while previews run SSR from the same repo.
- 2025-11-23 - [Features] **Scheda libro A4**: aggiunta pagina `/libri/schede/[slug]` stampabile con dettagli tecnici condivisi e footer editore.
- 2025-11-26 - [Features] **Recensioni libro**: sezione recensioni nella pagina libro con data, autore e link all’articolo completo.
- 2026-05-16 - [Refactoring] **D2 — Upgrade pacchetti non-Astro** (shape `2026-05-16-upgrade-and-env-security.md`): bump 16 patch/minor (astro 5.18, prettier 3.8, typescript 5.9, ecc.), switch `@datocms/cli` 3 → `datocms` 4, `@datocms/cma-client` 4 → 5.4.18, `@vercel/analytics`/`speed-insights` v2, swiper 11 → 12 (fix CVE critical), jsdom 27 → 29, esbuild 0.28, dotenv-cli 11, serialize-error 13. Build matrix static + preview pass; `astro check` 0 errori. Astro 6, Tailwind 4, TypeScript 6 fuori scope.
- 2026-05-16 - [Security] **D3 — Env vars security audit** (shape `2026-05-16-upgrade-and-env-security.md`): matrice 10 env var documentata in `docs/guidelines/security.md` con findings (DATOCMS_API_TOKEN dead-weight su Vercel, scope da verificare per PUBLIC token, 6 secret da migrare a Sensitive). Playbook manuale prodotto per azioni dashboard.
- 2026-05-16 - [Documentation] **D4 — DatoCMS skills review** (shape `2026-05-16-upgrade-and-env-security.md`): review CDA/Frontend/CMA/CLI in `docs/decision-log/2026-05-16-datocms-review.md`. Pattern canonici allineati. 9 recommendations: 2 shape candidates (gql.tada+auto-pagination, content-link), 4 follow-up TODO, 3 differiti.
- 2026-05-16 - [Refactoring] **Astro 6 migration** (shape `2026-05-16-astro-6-migration.md`): Astro 5.18 → 6.3.3, `@astrojs/vercel` 8 → 10. Rimossa integration `@astrojs/tailwind` (incompatibile), Tailwind 3 ora wired via PostCSS plugin (`postcss.config.mjs`). Build matrix static + preview pass, `astro check` 0 errori, smoke test su rendered HTML OK. 3 CVE risolti su 4 (`path-to-regexp` ReDoS resta transitive upstream da `@vercel/routing-utils`). Aggiunto `.vercel/` a `.prettierignore`.
- 2026-05-17 - [Refactoring] **Unify Vercel projects via ISR** (shape `2026-05-16-unify-projects-isr.md`): switch a `output:'server'` con `@astrojs/vercel` ISR config (`bypassToken`, `expiration:24h`, `exclude:/api/**`). Rimossa env var `SERVER` e branching (`src/lib/prerender.ts` eliminato, `src/lib/draftPreview.ts` semplificato, 20 file consumer aggiornati). Aggiunto endpoint `/api/revalidate` con `getAllPublicUrls()` helper, debouncing 5s, chunking 20-parallel. Dual-cookie draft mode (JWT app-level + `__prerender_bypass` Vercel-native). Visual Editing integrato (`@datocms/content-link` + `contentLink:'v1'` in CDA + boundaries sugli 8 block components Structured Text). Middleware CSP per iframe Visual Tab del plugin Web Previews. Slice A0-A9 completate. **Cutover in produzione 2026-05-30**: env var Vercel (`BYPASS_TOKEN`, `DATOCMS_BASE_EDITING_URL`) configurate, plugin Web Previews aggiornato via CLI (URL prod + `visualEditing.enableDraftModeUrl`), Webhook DatoCMS creato (id `33942`, eventi `item:publish/unpublish/delete`). Fix post-cutover (vedi `docs/decision-log/2026-05-30-isr-cutover-fixes.md`): CSP `frame-ancestors` esteso al dominio admin custom, `DATOCMS_BASE_EDITING_URL` corretto a URL bare. Azione residua non bloccante: decommissioning del secondo progetto Vercel.
